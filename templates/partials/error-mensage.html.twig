{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} flash-auto-hide">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
